[robotstudios.be]
; Unix user/group of processes
; Note: The user is mandatory. If the group is not set, the default user's group
;       will be used.
user = www-data
group = www-data
listen = /run/php/robotstudios.be-fpm.sock

listen.owner = www-data
listen.group = www-data
pm = static
pm.max_children = 1

